## 1. 專案基礎與目錄建立

- [x] 1.1 於專案根目錄建立 `README.md`，說明專案目的與學校電腦 MinGW/Dev-C++ 環境移轉與編譯命令指南。
- [x] 1.2 建立 `/include` 標頭檔目錄與 `/src` 實作檔目錄。
- [x] 1.3 建立 `/src/main.cpp`，首行強制呼叫 `system("chcp 65001 > nul")` 並建立基礎進入點以驗證編譯環境。

## 2. 核心資料模型實作 (Data Models)

- [x] 2.1 實作卡牌類別 `Card`（包含建構子、SP消耗、效果類型與數值定義），檔案置於 `/include/Card.h` 與 `/src/Card.cpp`。
- [x] 2.2 實作玩家類別 `Player`（包含 HP、Max HP、SP、Armor 護盾、反擊與眩暈狀態，以及抽牌/棄牌/洗牌牌組管理），檔案置於 `/include/Player.h` 與 `/src/Player.cpp`。
- [x] 2.3 實作怪物與 Boss 類別 `Monster`（包含生命值、防禦/受傷邏輯、招式庫與隨機下回合意圖生成邏輯），檔案置於 `/include/Monster.h` 與 `/src/Monster.cpp`。

## 3. 回合制卡牌戰鬥引擎 (Battle Mechanics)

- [x] 3.1 實作普通攻擊（回復 1 點 SP，造成基礎傷害）與卡牌使用 SP 檢查/扣除判定機制。
- [x] 3.2 實作六種卡牌核心效果邏輯：造成傷害、回復血量、增加護盾（抵扣下回合傷害）、反擊狀態附加、提升最大生命上限、怪物電暈（眩暈狀態）。
- [x] 3.3 實作怪物意圖預告顯示、怪物行動判定（檢查眩暈狀態），以及每回合結束時的狀態重置（例如護盾清零）。

## 4. 控制台 UI 與鍵盤輸入 (`conio.h`)

- [x] 4.1 實作基於 `_getch()` 的鍵盤監聽，讀取方向鍵（↑/↓）在手牌與普通攻擊選項間移動游標，按 Enter 鍵確認選擇，並忽略所有數字鍵輸入。
- [x] 4.2 實作戰鬥畫面更新渲染器（清屏重繪，依序輸出：怪物意圖、雙方狀態血條、行動日誌、手牌選單）。
- [x] 4.3 實作行動鎖定演出，在玩家或怪物出招時顯示詳細資訊並暫停 1 秒，此期間忽略任何鍵盤輸入。
- [x] 4.4 實作血條逐點減少動畫，利用 `std::this_thread::sleep_for` 逐步遞減 HP 數值並即時刷新控制台畫面。

## 5. 遊戲進程與通關判定 (Game Loop & Progression)

- [x] 5.1 實作關卡與回合控制器（共 3 大關，每關 3 回合，最後一回合為該關 Boss）。
- [x] 5.2 實作勝敗判定機制，包含玩家死亡顯示遊戲結束，以及擊敗第 3 關 Boss 顯示通關勝利。
- [x] 5.3 於 `/src/main.cpp` 中完整串接 Game 實體，編譯並進行多關卡完整測試。
