## 1. 實作控制台寬度輔助計算工具

- [x] 1.1 在 `include/Battle.h` 中聲明 UTF-8 顯示寬度測量函式 `getUTF8DisplayWidth` 與居中填充函式 `padUTF8Text`。
- [x] 1.2 在 `src/Battle.cpp` 中實作這兩個輔助函式，處理中英文字元混排時的寬度計算。

## 2. 重構手牌橫向排版繪製演算法

- [x] 2.1 在 `src/Battle.cpp` 的 `Battle::drawUI()` 中，實作每行最多並排 5 張卡牌的 Chunk 分塊繪製演算法（以多行字串並行輸出）。
- [x] 2.2 實作卡牌簡短描述（Short Description）的對照映射邏輯，使卡面描述文字可以完整塞入 14 格寬的卡框內。
- [x] 2.3 實作高亮框線判定：被選中的卡牌（當 `cursorIndex == i + 1`）繪製雙線邊框 `╔═════╗`，未被選中的卡牌繪製單線邊框 `┌─────┐`。
